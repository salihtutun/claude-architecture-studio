{
  "name": "claude-architecture-studio",
  "version": "1.0.0",
  "description": "Interactive system architecture and diagram design tool for Claude",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix frontend",
    "build:all": "npm run build --prefix frontend && npm run build --prefix server",
    "start": "node server/dist/index.js",
    "dev:server": "npm run dev --prefix server",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npm run dev:server & npm run dev:frontend"
  }
}
